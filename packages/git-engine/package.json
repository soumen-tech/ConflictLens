{
  "name": "@conflictlens/git-engine",
  "version": "1.0.0",
  "description": "ConflictLens Git & Conflict Analysis Engine — core/git, core/conflict, core/risk",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "analyze": "ts-node --project tsconfig.tsnode.json src/cli.ts",
    "typecheck": "tsc --noEmit",
    "lint:bom": "node scripts/check-no-bom.js"
  },
  "dependencies": {
    "@babel/parser": "^7.29.8",
    "@babel/traverse": "^7.29.8",
    "@conflictlens/shared": "*",
    "simple-git": "^3.27.0"
  },
  "devDependencies": {
    "@types/babel__traverse": "^7.28.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
